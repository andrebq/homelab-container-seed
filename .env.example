# Copy to .env and adjust values

# Where to find image definitions (comma-separated absolute paths)
SEEDER_DIRECTORIES=/seed_config

# Optional: also look in /<dir>/<SEEDER_INSTANCE_NAME>/ subdirs
SEEDER_INSTANCE_NAME=

# If the seed_config volume is empty, this repo is cloned at startup
SEED_CONFIG_REPO=
